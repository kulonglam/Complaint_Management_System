{
  "name": "complaint-management-backend",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Thin API for privileged operations (user invite, SLA jobs, email dispatch)",
  "main": "src/app.js",
  "scripts": {
    "start": "node src/app.js",
    "dev": "node --watch src/app.js",
    "seed": "node ../scripts/seed.mjs"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.57.4",
    "cors": "^2.8.5",
    "dotenv": "^17.2.2",
    "express": "^5.1.0",
    "express-rate-limit": "^8.1.0",
    "helmet": "^8.1.0"
  }
}
